# Set DATABASE_URL so Login, Register, and Forgot Password work without a backend.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
# After changing DATABASE_URL or pulling schema updates, run: python run_migration.py
# Backend API URL (only needed if not using DATABASE_URL for auth).
API_BASE=http://localhost:4000

# SMTP email settings for password-reset authentication (Brevo)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USERNAME=your_brevo_smtp_login
SMTP_PASSWORD=your_brevo_smtp_key
SMTP_FROM_EMAIL=your_verified_sender@domain.com
SMTP_USE_TLS=true

# PayPal Sandbox (set these to enable payments)
PAYPAL_MODE=sandbox
PAYPAL_CLIENT_ID=your_paypal_sandbox_client_id
PAYPAL_CLIENT_SECRET=your_paypal_sandbox_client_secret
PAYPAL_WEBHOOK_ID=your_paypal_webhook_id
# Optional: classic NVP/SOAP sandbox credentials (not used by current REST flow)
PAYPAL_NVP_USERNAME=your_paypal_nvp_username
PAYPAL_NVP_PASSWORD=your_paypal_nvp_password
PAYPAL_NVP_SIGNATURE=your_paypal_nvp_signature

# Streamlit app port (default in .streamlit/config.toml is 8080). Override: streamlit run main.py --server.port 8501
# SERVER_PORT=8080
